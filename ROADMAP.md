# Pavilo / 语亭路线图

> 一条命令，给身边的人一间聊天室；按需组装，为自己的产品接入讨论空间。

Pavilo 是**浏览器即用、默认临时、可自托管、可按需组装的轻量聊天工具，也能为已有产品提供方便接入的聊天与玩法能力**。独立聊天室与接入共用一套内核、一条主线。接入是把已经在跑的语亭页面放进对方的网页容器，契约见 [接入说明](docs/integrate.md)。不发布 SDK。

本路线图于 2026-09-21 调整。版本是目标安排，不是日历日期承诺；门槛未达成时继续交付兼容增量，不压缩验收。设计依据见 [ADR-0008](docs/adr/0008-embeddable-composable-chat.md)，架构见 [演进文档](docs/evolution.md)，接入边界见 [集成设计（规划中）](docs/integration.md)。

## 产品目标与维护分工

- **独立使用者**：启动 Node.js 或 Docker，同网段或私有网络中的人打开浏览器即可交谈；默认不持久化，停止服务后聊天记录回到空白。
- **个人开发者与小型社区维护者**：复用宿主登录，选择聊天功能，为已有产品接入预建讨论频道。
- **玩法与扩展贡献者**：按照稳定契约开发 Play、Agent、审核策略和事件扩展。

维护者主要建设内核、存储、接入、扩展契约、基础治理、开发者体验和发布保障。官方狼人杀是完整参考实现，与基础设施并行开发，必须在 v2.0 前完成。社区可按契约开发更多玩法，无需理解或修改整个内核。

“一键接入”意味着：**部署 Pavilo、配置宿主信任关系、复制少量接入代码即可使用，不要求修改 Pavilo 源码**。它不意味着免部署、免鉴权配置或由官方提供云服务。

## 当前基线与历史

以下状态以本次梳理时的标签和主分支实现为准；功能发布后应同步更新本表。

| 状态 | 能力 |
| --- | --- |
| 已发布：v1.0.0 | 默认临时、多频道、ACK/幂等/重连、严格配置、资源边界、Protocol v4、源码与 Docker 部署 |
| 已发布：v1.1.0 | 可选 SQLite、按天留存、数据库迁移、事务提交后 ACK |
| 已发布：v1.2.0 | 历史分页、prune、备份/恢复、完整性检查、存储统计 |
| 已发布：v1.3.0 | 进程内 AI 网关、管理后台、房间/频道配置覆盖层、人员席位管理、禁言/请离/IP 黑名单、Play/Agent 宿主与 echo 契约夹具 |
| 已发布：v1.4.0 | 功能目录、宿主 JWT 身份与频道授权 |
| 已发布：v1.5.0 | 治理闭环：举报、墓碑移除、稳定用户拒绝、操作记录 |
| 已发布：v1.6.0（最新稳定版） | 嵌入预览：宿主模式 iframe、跨来源登录与玩法往返 |
| 进行中 | 一段话接入：网页 iframe，以及 App 的 WebView。不含小程序。不发布 SDK。见 [接入说明](docs/integrate.md) |
| 计划中 | 提交前文本规则、官方狼人杀 |

v0.x 的质量、协议收口、部署与发布工程记录以及各稳定版的实际变更保留在 [CHANGELOG](CHANGELOG.md)。不把后续规划能力追记为已发布功能。

v1.5.0 发布验证（2026-09-23，本机 Node v26.5.0）：`npm test` 421 个、420 通过、1 跳过；`npm run test:browser` 19 通过。v1.6.0 发布验证（2026-09-23，本机 Node v26.5.0）：`npm test` 426 个、425 通过、1 跳过；`npm run test:browser` 20 通过。这是当次记录，不是未来版本的固定测试数量。浏览器检查从 v1.6.0 起挡住发布。Node 24/26 仍允许失败，不能写成已经挡住全部版本矩阵。

## 长期不变量

1. **Ephemeral First**：默认无数据库、无账号、无 AI 依赖；临时模式是完整产品模式。使用“默认不持久化”，不使用“无内存”。
2. **One Mainline**：独立/嵌入是交付形态，memory/sqlite 是存储模式，不拆产品版本或维护两套内核。
3. **Optional Means Optional**：能力按需启用，依赖可见、配置可校验；不开启时不强制连接外部服务。
4. **Core Before Platform**：网络、SQL、厂商调用和具体玩法规则留在适配层或模块中。
5. **Compatibility Is a Feature**：应用、配置、协议、数据库及未来 SDK/扩展契约分别演进。
6. **Safe by Construction**：身份、授权、内容治理各司其职；不把可嵌入或配置 Origin 当作可安全裸露公网的证明。
7. **No Premature Generalization**：以实际接入示例和官方狼人杀验证边界，不先造通用游戏引擎、沙箱或市场。

## 目标版本与并行工作

| 阶段 | 基础设施主线 | 玩法并行线 | 完成门槛 |
| --- | --- | --- | --- |
| v1.3：收口现有基础 | 整理并发布网关、后台、人员管理与 Play 宿主；修正文档漂移 | 狼人杀状态机、页面和 Agent | 实现有契约测试、准确文档和发布记录 |
| v1.4：组装与身份基础 | 功能目录、依赖校验、有效能力查询；稳定用户、宿主凭证、频道权限 | 身份恢复、私密视图、人机混编验证 | 匿名与宿主身份模式成立，服务端执行功能和权限限制 |
| v1.5：治理闭环 | 举报、墓碑式移除、按稳定 userKey 拒绝、值班台操作记录。不改 ACK 管道，不做模型审核 | 协作者继续狼人杀，不作为本版门槛 | 值班台能处理举报并移除内容；引用和在线客户端不再展示已移除正文；拒绝名单随 moderation 段生效 |
| v1.6：嵌入预览 | 宿主模式嵌入页，只对配置的宿主来源放开嵌入。浏览器检查改为阻塞发布。不是正式 SDK | 聊天与玩法切换留在嵌入区域 | 跨来源示例走通登录和讨论；反复挂载不残留连接 |
| 下一次发布：一段话接入 | 不发布 SDK。网页继续用 iframe 消息桥；App 用 WebView 打开 `/embed`，凭证只放 fragment。不含小程序 | 狼人杀不作为门槛 | 只按 [接入说明](docs/integrate.md) 接上；query 里的凭证不能登录；打开后地址栏不留下凭证 |
| 其后 | 提交前文本规则。值班台同步词表，命中则不 ACK。不抽通用 Policy 端口，不做视觉审核 | 狼人杀应在契约冻结前完成 | 词表拒绝有测试；Agent 公开发言走同一检查 |
| v2.0：稳定集成交付 | 先做必要架构迁移，再冻结接入契约。此前的 SDK 保持实验 | 狼人杀作为稳定参考随版本交付 | 不改核心源码即可组装、集成、升级 |

依赖顺序是：**先收口已有能力 → 建立组装与身份边界 → 验证嵌入和治理 → 稳定公共契约**。狼人杀始终并行；不等待全部玩法完成才开始身份与集成，也不在缺少官方完整参考时宣布 v2.0 达标。

## v2.0 必须具备的能力

### 组装与预设

- 提供临时聊天室、持久社区、嵌入聊天、聊天＋玩法四类预设。
- 实例级管理存储、身份接入、网关和已安装模块；频道级管理聊天功能、访问权限、审核和玩法绑定。
- 图片、回复、回应、提及、输入提示和历史访问的开关在服务端执行，客户端根据有效能力呈现。
- 启动或保存配置时验证依赖；实例决定存储，Play 当前仍要求 SQLite。身份验证本身不强制 SQLite，不承诺任意组合都有效。

### 宿主身份与预建频道

- 保留可选访客；宿主后端签发短期凭证，Pavilo 验证稳定身份与授权频道，不自建注册、密码和找回系统。
- 区分用户、会话、连接和 Agent 席位；支持续期、退出、切换身份、凭证过期，后台凭证不交给普通客户端。
- 权限覆盖频道发现、加入、历史、发送和玩法动作；只接入通过配置或后台预建的频道，不提供宿主动态建频道接口。

### 现成界面与接入

- 不发布 SDK、npm 包或框架组件，也不提供托管服务器。对方在自己的部署里启动 Pavilo，不改它的源码。
- 网页用 iframe 打开 `/embed`，身份走 `postMessage`。App 用系统 WebView 打开同一页，身份只放在 fragment，页面立刻从地址栏移除。
- 不写原生界面，也不把语亭嵌进对方的 Node 进程。小程序不在当前范围。
- 功能开关留在对方自己的 Pavilo 配置里。接入方启动这个服务、签发凭证、打开页面。
- 身份不进 query、路径或第三方 Cookie。嵌入来源和「允许顶层打开」分开配置。后台保持禁止嵌入。
- 聊天与玩法切换留在同一个网页容器里。契约以 [接入说明](docs/integrate.md) 为准。

### 治理、扩展与官方样例

- 官方提供访问控制、限流、禁言、封禁、举报、管理员移除内容和操作记录；移除标记在历史、回复和在线客户端保持一致，不扩展为完整消息编辑。
- SQLite 持久社区保留治理记录，临时模式遵守临时数据边界。
- 同步文本规则若已交付，命中、超时按当时写明的策略处理，拒绝不能发出成功 ACK。通用 Policy 端口等第二个消费方出现再抽。视觉审核不作为 v2.0 门槛。
- 提交后事件失败不回滚消息；队列、超时和错误可观测。不在 v1.5 改这条管道。
- 身份、授权和 Play 使用明确端口。人和 Agent 的公开消息走同一套权限与限额；文本规则落地后也走这条路径。
- 扩展是部署者安装的可信代码，有声明、配置校验、生命周期、兼容版本、示例和契约测试；不承诺恶意代码或无限循环的进程隔离。
- 官方狼人杀支持人机混编、确定性规则、私密信息、断线恢复和模型失败处理，作为可照着开发的完整 Play 示例。

## v2.0 发布验收

- 临时模式、SQLite 留存/迁移、ACK、幂等、重连回归通过；默认部署不被新增可选能力拖重。
- 关闭功能后绕过 UI 仍被拒绝；依赖缺失报错清晰。
- 身份伪造、过期、续期、退出、切换、频道越权和历史越权均有测试。
- 宿主与 Pavilo 在不同来源下完成真实接入；第三方 Cookie 禁用时仍可认证；拆掉 iframe 或网页容器后不留下连接。
- 举报、内容移除和稳定用户拒绝保持有效；Agent 的公开消息不绕过它们。文本规则若已交付，拒绝不能 ACK。视觉审核和通用 Policy 端口不是本里程碑的门槛。
- 狼人杀完整对局、非法动作、私密信息、恢复和模型失败验收通过。
- 支持的 Node 版本矩阵与关键浏览器验收阻塞发布；接入说明、示例配置、源码与容器启动经过 smoke test。
- 迁移、备份恢复和回滚说明可复现；旧版本数据库不能直接交给不兼容旧进程回滚，按兼容说明或升级前备份恢复。
- 中英文入口、当前实现文档、未来设计、版本状态、贡献指南一致；不以历史测试数代替当次检查。

成功以接入者能否独立部署、组装、集成和升级，以及贡献者能否按契约交付玩法衡量。记录示例接入的步骤、耗时和失败点来改进体验；Star 数量不作为功能验收指标。

## 版本与迁移原则

v1.x 保持已承诺的 Protocol v4 和既有配置兼容。接入说明按兼容承诺演进，不能把已稳定的 Play 契约重新当作任意可破坏的内部接口。不另发布 SDK。

v2.0 是稳定集成里程碑，允许为身份、配置和公共边界集中进行必要架构调整；不人为制造破坏，也不在没有实现依据时预定新协议号。每项破坏性变化单列 ADR、废弃说明、迁移和回滚路径。应用升主版本不意味着所有协议和 Schema 同时升版。发布程序见 [版本管理](docs/version-management.md)。

## 明确暂缓

v2.0 不包含接入 SDK、npm 包、框架组件、无界面客户端、深度组件替换、Node 后端库嵌入、完整账号系统、宿主动态建频道接口、多租户、插件安装市场、沙箱、私聊、音视频、分布式存储。一个实例优先服务一个应用，多个频道不等于租户隔离。

小程序不在当前接入范围。原生应用用 WebView 打开同一页，不另做原生界面或系统推送。全文搜索、文件管理、端到端加密、联邦、远程数据库和大规模集群留在后续需求池，不挤占接入主线。

## 当前执行顺序

1. v1.6.0 已发布：嵌入预览。见 [v1.6 设计](docs/v1.6-design.md)。浏览器检查已改为阻塞发布。
2. 下一次发布是一段话接入。见 [接入说明](docs/integrate.md)。不发布 SDK。词表、视觉审核和通用 Policy 端口继续让路。狼人杀由协作者并行，不作为这一次的门槛。
3. 接入说明稳定之后，再考虑词表。不把 npm 包当作接入的前提。
